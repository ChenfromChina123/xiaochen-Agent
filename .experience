1、Agent 支持全局运行：使用 ~/.xiaochen_agent_v2 作为全局数据根目录，存储配置、日志和缓存。
2、两级规则加载：Agent 会同时加载全局规则 (~/.xiaochen_agent_v2/userrules) 和项目本地规则 (./userrules)，增强环境感知。
3、安装脚本路径修复：优化了 install.sh 和 install.ps1，使用标记位确保 .bashrc 清理彻底，防止语法冲突。
4、Git 追踪优化：已将 config.json 从 Git 追踪中移除，防止 API Key 泄露。
5、剪贴板静默处理：在 Linux 无图形界面环境下自动禁用剪贴板检测，消除调试噪音。
