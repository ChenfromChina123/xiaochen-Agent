1. 优化注入机制：确保 Agent 可以从任意目录启动并正确识别当前工作目录。
2. 全局配置管理：将配置文件和日志存储移动到全局目录（~/.xiaochen_agent_v2），避免在运行目录产生垃圾文件。
3. 增强项目感知：在启动时自动向 Agent 注入当前目录的文件树结构（Tree），解决 Agent “不敢”或“不知道”操作当前目录的痛点。
4. 两级规则支持：支持全局规则（Global Rules）和项目本地规则（Local Rules），兼顾通用习惯和特定项目需求。
5. 多种启动方式：支持 `agent` 直接启动（当前目录）或 `agent <path>` 指定目录启动。
6. 一键安装体验：提供 PowerShell 安装脚本，支持通过 `irm | iex` 快速配置全局环境变量。
